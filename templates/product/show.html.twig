{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}

    <h1>{{ product.name }}</h1>
    <p>{{ product.price /100}}</p>
    <p>{{ product.description}}</p>

{% endblock %}



